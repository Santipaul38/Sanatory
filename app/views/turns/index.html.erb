<p id="notice"><%= notice %></p>

<h1>Turns</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Hour</th>
      <th>Medic</th>
      <th>Patient</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @turns.each do |turn| %>
      <tr>
        <td><%= turn.date.strftime("%m/%d/%Y") %></td>
        <td><%= turn.date.strftime("%I:%M%p") %></td>
        <td><%= turn.medic.name %></td>
        <td><%= turn.patient.name %></td>
        <td><%= turn.price %></td>
        <td><%= link_to 'Show', turn %></td>
        <td><%= link_to 'Edit', edit_turn_path(turn) %></td>
        <td><%= link_to 'Destroy', turn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Turn', new_turn_path %>
<br>
<strong> <%= link_to "Back", welcome_index_path %> </strong>
